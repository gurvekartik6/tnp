import { useEffect, useMemo, useState } from 'react';
import { ArrowRight, BriefcaseBusiness, FileText, Search, Sparkles, X } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
const items=[['Placements','/placements','Open published placement drives and placement information.',BriefcaseBusiness],['Dean Admin','/login','Manage the public website, statistics, calendar, media and documents.',Sparkles],['Recruiter Documents','/recruiters/documents','JAF, FRS, placement brochure and recruiter resources.',FileText],['Placement Statistics','/statistics','Dynamic placement trends and department analytics.',Sparkles],['Announcements','/announcements','Latest published T&P updates and notices.',FileText]] as const;
export default function CommandPalette(){const navigate=useNavigate();const [open,setOpen]=useState(false);const [query,setQuery]=useState('');const results=useMemo(()=>items.filter(([label,,description])=>`${label} ${description}`.toLowerCase().includes(query.toLowerCase())).slice(0,7),[query]);useEffect(()=>{const onOpen=()=>setOpen(true);window.addEventListener('sggs-open-command',onOpen);const onKey=(e:KeyboardEvent)=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();setOpen(true)}if(e.key==='Escape')setOpen(false)};window.addEventListener('keydown',onKey);return()=>{window.removeEventListener('keydown',onKey);window.removeEventListener('sggs-open-command',onOpen)}},[]);const go=(path:string)=>{setOpen(false);setQuery('');navigate(path)};return <><button className="global-search-trigger" onClick={()=>setOpen(true)} aria-label="Search SGGSIE&T T&P"><Search size={16}/><span>Search SGGS T&amp;P</span><kbd>Ctrl K</kbd></button>{open&&<div className="command-overlay" onMouseDown={()=>setOpen(false)}><div className="command-panel" onMouseDown={e=>e.stopPropagation()} role="dialog" aria-modal="true"><div className="command-head"><Search size={18}/><input autoFocus value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search pages, documents, statistics…"/><button onClick={()=>setOpen(false)} aria-label="Close search"><X size={18}/></button></div><div className="command-hint"><span>Quick navigation</span><span>Esc to close</span></div><div className="command-results">{results.length?results.map(([label,path,description,Icon])=><button className="command-item" key={path} onClick={()=>go(path)}><span className="command-icon"><Icon size={16}/></span><span><b>{label}</b><small>{description}</small></span><ArrowRight size={15}/></button>):<div className="command-empty">No matching destination.</div>}</div></div></div>}</>}
