import { usePortal } from '../PortalContext';
export default function SettingsModule(){const {role,notify}=usePortal();return <section className="module"><div className="module-head"><div><span className="eyebrow">{role.toUpperCase()} / SETTINGS</span><h2>Workspace settings</h2><p>Manage preferences without changing the underlying data model.</p></div></div><div className="settings-grid">{[['Notifications','In-app alerts enabled','Manage notification preferences'],['Security','Demo authentication active','Replace with institutional SSO later'],['Appearance','Light institutional theme','Deep navy, blue and academic gold'],['Data','Admin-editable placeholders','Official figures remain protected']].map(x=><div key={x[0]}><span className="eyebrow">{x[0]}</span><h3>{x[1]}</h3><p>{x[2]}</p><button className="btn-secondary" onClick={()=>notify(`${x[0]} settings opened`)}>Manage</button></div>)}</div></section>}
